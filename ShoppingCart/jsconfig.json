{
  "compilerOptions": {
    "baseUrl": ".", // Ensures paths are resolved relative to the project root
    "paths": {
      "@/*": ["src/*"] // Maps `@/` to `src/*`
    }
  },
  "exclude": ["node_modules", "dist"]
}